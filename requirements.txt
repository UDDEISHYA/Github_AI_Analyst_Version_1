# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.100
uvicorn[standard]>=0.23
python-multipart>=0.0.6         # File upload support for FastAPI

# ── LLM Providers (need at least one) ────────────────────────────────────────
anthropic>=0.25                 # Claude API client
openai>=1.12                    # GPT API client

# ── Data & Analysis ──────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scipy>=1.10
duckdb>=0.9
statsmodels>=0.14               # Power analysis, OLS, multiple comparisons
scikit-learn>=1.3               # Logistic regression for propensity score matching
pyfixest>=0.25                  # DiD, event studies, fixed effects

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12

# ── Utilities ────────────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0              # Environment variable management
python-docx>=1.1                # Document generation

# ── Database Connectors (optional) ───────────────────────────────────────────
psycopg2-binary>=2.9            # Postgres database connectivity
snowflake-connector-python>=3.0 # Snowflake database connectivity
